<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.chatbot.tools.user.mapper.UserMapper">

    <select id="findById" resultType="com.example.chatbot.tools.user.entity.User">
        SELECT user_id,
               name,
               email,
               phone,
               address
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="findByName" resultType="com.example.chatbot.tools.user.entity.User">
        SELECT user_id,
               name,
               email,
               phone,
               address
        FROM users
        WHERE name LIKE '%' || #{name} || '%'
        ORDER BY name
        FETCH FIRST 20 ROWS ONLY
    </select>

</mapper>
